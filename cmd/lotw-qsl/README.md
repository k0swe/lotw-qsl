Command-line utility to run a LotW query. This is mostly a demo of 
the OpenAPI generated client.

## Usage

```shell
lotw-qsl -username $LOTWUSER -password $LOTWPASS
```

The returned data is in ADIF format.
